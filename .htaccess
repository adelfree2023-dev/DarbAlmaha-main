# ============================================
# DarbAlmaha - Performance & Security .htaccess
# ============================================

# Enable Rewrite Engine
<IfModule mod_rewrite.c>
    RewriteEngine On
    # ============================================
    # SEO REDIRECTS FOR REORGANIZED DIRECTORIES
    # ============================================
    # Redirect old root locations to /ar/ locations
    RewriteRule ^doha/?$ /ar/doha/ [R=301,L]
    RewriteRule ^al-wakra/?$ /ar/al-wakra/ [R=301,L]
    RewriteRule ^al-rayyan/?$ /ar/al-rayyan/ [R=301,L]
    RewriteRule ^al-khor/?$ /ar/al-khor/ [R=301,L]
    RewriteRule ^lusail/?$ /ar/lusail/ [R=301,L]
    RewriteRule ^umm-salal/?$ /ar/umm-salal/ [R=301,L]
    RewriteRule ^al-gharafa/?$ /ar/al-gharafa/ [R=301,L]
    RewriteRule ^the-pearl/?$ /ar/the-pearl/ [R=301,L]

    # Redirect old root hubs and subpages
    RewriteRule ^services/?$ /ar/services/ [R=301,L]
    RewriteRule ^services/(.*)$ /ar/services/$1 [R=301,L]
    RewriteRule ^booking/?$ /ar/booking/ [R=301,L]
    RewriteRule ^booking/(.*)$ /ar/booking/$1 [R=301,L]
    RewriteRule ^pricing/?$ /ar/pricing/ [R=301,L]
    RewriteRule ^pricing/(.*)$ /ar/pricing/$1 [R=301,L]
    RewriteRule ^blog/?$ /ar/blog/ [R=301,L]
    RewriteRule ^blog/(.*)$ /ar/blog/$1 [R=301,L]

    # Redirect old policy files
    RewriteRule ^privacy-policy\.html$ /ar/privacy-policy.html [R=301,L]
    RewriteRule ^terms\.html$ /ar/terms.html [R=301,L]
    RewriteRule ^refund-policy\.html$ /ar/refund-policy.html [R=301,L]

    # ============================================

    # ============================================
    # FORCE HTTPS (SSL is active via Let's Encrypt)
    # ============================================
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # Force non-www to www (choose ONE version for consistency)
    # Uncomment if you want www version:
    # RewriteCond %{HTTP_HOST} !^www\. [NC]
    # RewriteRule ^(.*)$ https://www.%{HTTP_HOST}/$1 [R=301,L]

    # Remove trailing slash (for non-directories)
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [R=301,L]
</IfModule>

# ============================================
# PERFORMANCE: Gzip Compression
# ============================================
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json application/xml application/rss+xml application/atom+xml application/manifest+json application/ld+json
</IfModule>

# ============================================
# PERFORMANCE: Browser Caching
# ============================================
<IfModule mod_expires.c>
    ExpiresActive On

    # Images - 1 year
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresByType image/vnd.microsoft.icon "access plus 1 year"

    # Fonts - 1 year
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/ttf "access plus 1 year"
    ExpiresByType font/otf "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    ExpiresByType application/font-woff2 "access plus 1 year"

    # CSS & JS - 1 month
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/x-javascript "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"

    # HTML - 1 hour (for frequent updates)
    ExpiresByType text/html "access plus 1 hour"

    # Default - 1 week
    ExpiresDefault "access plus 1 week"
</IfModule>

# Cache-Control headers
<IfModule mod_headers.c>
    # Images
    <FilesMatch "\.(webp|jpg|jpeg|png|gif|svg|ico)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>

    # Fonts
    <FilesMatch "\.(woff2|woff|ttf|otf|eot)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>

    # CSS & JS
    <FilesMatch "\.(css|js)$">
        Header set Cache-Control "public, max-age=2592000"
    </FilesMatch>

    # HTML & Text (Allow AI Crawlers CORS Access)
    <FilesMatch "\.(html|htm|txt)$">
        Header set Cache-Control "public, max-age=3600"
        Header set Access-Control-Allow-Origin "*"
    </FilesMatch>
</IfModule>

# ============================================
# PERFORMANCE: Serve WebP Images
# ============================================
<IfModule mod_rewrite.c>
    # Serve WebP if browser supports it
    RewriteCond %{HTTP_ACCEPT} image/webp
    RewriteCond %{DOCUMENT_ROOT}/$1.webp -f
    RewriteRule ^(img/.+)\.(jpg|jpeg|png|gif)$ $1.webp [T=image/webp,L]
</IfModule>

# WebP MIME type
<IfModule mod_mime.c>
    AddType image/webp .webp
</IfModule>

# ============================================
# SECURITY HEADERS
# ============================================
<IfModule mod_headers.c>
    # Content Security Policy
    Header set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://www.googletagmanager.com https://connect.facebook.net https://analytics.tiktok.com https://sc-static.net; style-src 'self' https://fonts.googleapis.com 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://www.googletagmanager.com https://www.facebook.com https://analytics.tiktok.com https://sc-static.net; frame-ancestors 'none'; base-uri 'self'; form-action 'self';"

    # Referrer Policy
    Header set Referrer-Policy "strict-origin-when-cross-origin"

    # X-Content-Type-Options
    Header set X-Content-Type-Options "nosniff"

    # X-Frame-Options
    Header set X-Frame-Options "DENY"

    # X-XSS-Protection
    Header set X-XSS-Protection "1; mode=block"

    # HSTS (uncomment when SSL is enabled)
    # Header set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

    # Permissions-Policy
    Header set Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=(), usb=()"

    # Remove server signature
    Header unset Server
    Header unset X-Powered-By
</IfModule>

# Disable server signature
ServerSignature Off

# ============================================
# PREVENT DIRECTORY LISTING
# ============================================
Options -Indexes

# ============================================
# BLOCK HIDDEN FILES & DIRECTORIES (.git, .env, etc.)
# ============================================
<IfModule mod_rewrite.c>
    RewriteRule (^\.|/\.) - [F]
</IfModule>

# ============================================
# BLOCK SENSITIVE FILE TYPES
# ============================================
<FilesMatch "\.(env|git|htaccess|htpasswd|ini|log|sh|sql|swp|dist|backup|old|bak|yml|yaml|toml)$">
    Require all denied
</FilesMatch>

# ============================================
# ERROR PAGES (customize if needed)
# ============================================
# ErrorDocument 404 /404.html
# ErrorDocument 403 /403.html
# ErrorDocument 500 /500.html

# ============================================
# UTF-8 ENCODING
# ============================================
AddDefaultCharset UTF-8
<IfModule mod_mime.c>
    AddCharset UTF-8 .html .css .js .xml .json .webp
</IfModule>
